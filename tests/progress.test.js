import { describe, it } from 'vitest'

// Milestone 3 — see plan section 5.
describe('progress', () => {
  it.todo('getRequiredForEnding combines commonRequired, groups, and extraRequired')
  it.todo('getGroupStatus is satisfied when done >= pick')
  it.todo('a boss that is both required and an extra is counted once')
  it.todo('switching endings keeps the defeated list')
  it.todo('Morgott counts as fixed-required and as a Great Rune group option')
  it.todo('every slug in endings.json exists in the boss snapshot')
})
